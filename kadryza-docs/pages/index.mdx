---
title: Documentation Kadryza — API de paiement Mobile Money
---

import { Callout, Steps } from 'nextra/components'
import { Zap, Radio, Package, Lock, Bell, AlertTriangle, LayoutDashboard, ArrowRight, CheckCircle2, Shield, Clock } from 'lucide-react'

<div className="hero-section">
  <div className="hero-badge">
    <Shield size={14} />
    <span>API sécurisée & certifiée</span>
  </div>
  <h1 className="hero-title">
    <span className="kadryza-gradient-text">Documentation Kadryza</span>
  </h1>
  <p className="hero-subtitle">
    L'infrastructure de paiement Mobile Money pour le Tchad et la zone CEMAC.
    <br />
    Intégrez les paiements <strong>Airtel Money</strong> et <strong>Moov Money</strong> en quelques minutes.
  </p>
  <div className="hero-stats">
    <div className="stat-item">
      <Clock size={16} className="stat-icon" />
      <span><strong>5 min</strong> d'intégration</span>
    </div>
    <div className="stat-item">
      <CheckCircle2 size={16} className="stat-icon" />
      <span><strong>99.9%</strong> uptime</span>
    </div>
    <div className="stat-item">
      <Shield size={16} className="stat-icon" />
      <span><strong>HMAC-SHA256</strong> sécurisé</span>
    </div>
  </div>
</div>

## Commencez ici

<div className="card-grid">
  <a href="/quickstart" className="feature-card">
    <div className="card-icon card-icon-orange">
      <Zap size={22} />
    </div>
    <h3>Démarrage rapide</h3>
    <p>Intégrez Kadryza dans votre projet en 5 minutes. Installation, configuration, premier paiement.</p>
    <span className="card-link">Commencer <ArrowRight size={14} /></span>
  </a>
  <a href="/api-reference" className="feature-card">
    <div className="card-icon card-icon-blue">
      <Radio size={22} />
    </div>
    <h3>Référence API</h3>
    <p>Endpoints REST complets : transactions, webhooks, authentification. Exemples curl et SDK.</p>
    <span className="card-link">Explorer <ArrowRight size={14} /></span>
  </a>
  <a href="/sdk" className="feature-card">
    <div className="card-icon card-icon-purple">
      <Package size={22} />
    </div>
    <h3>SDK JavaScript</h3>
    <p>Le SDK officiel @kadryza/sdk pour Node.js. Méthodes, types, gestion d'erreurs.</p>
    <span className="card-link">Découvrir <ArrowRight size={14} /></span>
  </a>
</div>

---

## Comment ça marche

<Steps>

### Créez un compte merchant

Inscrivez-vous sur le [dashboard Kadryza](https://dashboard.kadryza.app/register) pour obtenir un accès à l'environnement de production.

### Obtenez une clé API

Dans le dashboard, accédez à **API Keys** → **Créer une clé**. Votre clé au format `kadryza_live_` est générée instantanément.

<Callout type="warning">
  Votre clé API n'est affichée qu'**une seule fois** à la création. Copiez-la et stockez-la dans un endroit sécurisé.
</Callout>

### Initiez votre premier paiement

```javascript
import Kadryza from '@kadryza/sdk'

const kadryza = new Kadryza({
  apiKey: process.env.KADRYZA_API_KEY
})

const transaction = await kadryza.transactions.initiate({
  reference: 'order_abc123',
  amount: 5000,
  currency: 'XAF',
  operator: 'AIRTEL',
  phone_number: '+23566000000',
  description: 'Paiement commande #ABC123'
})

console.log(transaction.id)          // UUID de la transaction
console.log(transaction.status)      // "PENDING"
console.log(transaction.internal_ref) // "KADRYZA-XXXXXXXX"
```

</Steps>

---

## Opérateurs supportés

| Opérateur | Pays | Devise | Statut |
|-----------|------|--------|--------|
| **Airtel Money** | Tchad | XAF (Franc CFA CEMAC) | ✅ Disponible |
| **Moov Money** | Tchad | XAF (Franc CFA CEMAC) | ✅ Disponible |

---

## Ressources utiles

<div className="card-grid">
  <a href="/authentication" className="feature-card">
    <div className="card-icon card-icon-green">
      <Lock size={22} />
    </div>
    <h3>Authentification</h3>
    <p>Comprendre les clés API, les headers HTTP, et les bonnes pratiques de sécurité.</p>
    <span className="card-link">En savoir plus <ArrowRight size={14} /></span>
  </a>
  <a href="/guides/webhook-verification" className="feature-card">
    <div className="card-icon card-icon-yellow">
      <Bell size={22} />
    </div>
    <h3>Guide Webhooks</h3>
    <p>Recevez les notifications de paiement en temps réel. Vérification de signature incluse.</p>
    <span className="card-link">Configurer <ArrowRight size={14} /></span>
  </a>
  <a href="/errors" className="feature-card">
    <div className="card-icon card-icon-red">
      <AlertTriangle size={22} />
    </div>
    <h3>Codes d'erreur</h3>
    <p>Liste complète des erreurs API et SDK avec leurs solutions concrètes.</p>
    <span className="card-link">Consulter <ArrowRight size={14} /></span>
  </a>
  <a href="https://dashboard.kadryza.app" className="feature-card">
    <div className="card-icon card-icon-orange">
      <LayoutDashboard size={22} />
    </div>
    <h3>Dashboard</h3>
    <p>Gérez vos transactions, clés API et webhooks depuis l'interface d'administration.</p>
    <span className="card-link">Ouvrir <ArrowRight size={14} /></span>
  </a>
</div>

<Callout type="info">
  **Besoin d'aide ?** Consultez les [guides d'intégration](/guides/nextjs) pour Next.js et Express.js,
  ou envoyez-nous un message à **support@kadryza.app**.
</Callout>
